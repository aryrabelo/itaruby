/target
tmp/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
*.gate.lock*

# Issue tracker stays local-only in this repo — never versioned.
.beads/

# Private corpus path mapping — points at machine-local paths, never versioned.
scripts/corpora-local.txt

# Generated by bd-dashboard, regenerable
plans/beads-board.html

# Bora scratch space (per-pane conversation log) — never versioned
scratch/
# Bug/replay classifier run artifact — regenerable, never versioned
scripts/bug-replay/results/
# Local Serena MCP cache — tool artifact, never versioned
.serena/

# Paseo tool state — never versioned
.paseo/
