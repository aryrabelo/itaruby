# typed: true
# DO NOT EDIT MANUALLY — toy fixture for bead ita-vto, mirroring the shape
# Tapioca actually emits: one fully-qualified `class`/`module` header per
# declaration, never nested blocks. Names are invented, not a real gem.

class TapiocaVtoFixtureGem::Widget
end

class TapiocaVtoFixtureGem::Base
end
